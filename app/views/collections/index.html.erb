<p id="notice"><%= notice %></p>

<h1>Collections</h1>

<div class="search">
    <h3>Search for Collection by Account Number</h3>
    <%= form_with(url: collections_path, method: "get", local: true) do %>
      <%= text_field_tag(:search, params[:search]) %>
      <%= submit_tag ("Search") %><br>
    <% end %>
  </div>
<% if current_user && current_user.admin %>
  <p>something about showing all</p>
<% end %>


<% if @collection %>

<table>
  <thead>
    <tr>
      <th>Account number</th>
      <th>Lname</th>
      <th>Fname</th>
      <th>Money due</th>
      <th>Money paid</th>
      <th>Total due</th>
      <th>Phone</th>
      <th>Phone2</th>
      <th>Phone3</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @collections.each do |collection| %>
      <tr>
        <td><%= @collection.account_number %></td>
        <td><%= @collection.lname %></td>
        <td><%= @collection.fname %></td>
        <td><%= @collection.money_due %></td>
        <td><%= @collection.money_paid %></td>
        <td><%= @collection.total_due %></td>
        <td><%= @collection.phone %></td>
        <td><%= @collection.phone2 %></td>
        <td><%= @collection.phone3 %></td>
        <td><%= @collection.notes %></td>
        <td><%= link_to 'Show', @collection %></td>
        <td><%= link_to 'Edit', edit_collection_path(@collection) %></td>
        <td><%= link_to 'Destroy', @collection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>
<% end %>
<br>

<%= link_to 'New Collection', new_collection_path %>
